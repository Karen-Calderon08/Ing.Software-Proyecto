javascript
const express = require('express');
const app = express();

// Importar la función ValidarOrdenPedido
const { ValidarOrdenPedido } = require('./ruta-de-tu-funcion');

app.get('/validar-orden', (req, res) => {
  // Llamar a la función ValidarOrdenPedido y manejar la respuesta
  // Aquí puedes incluir la lógica para llamar a tu función y manejar la respuesta
  res.send('¡Ruta de validación de orden creada con éxito!');
});

// Iniciar el servidor en el puerto 3000
app.listen(3000, () => {
  console.log('Servidor iniciado en http://localhost:3000');
});
